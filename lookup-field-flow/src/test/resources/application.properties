# Run the Vaadin test server in development mode so the frontend bundle is built
# at runtime from the actual running application. This ensures components used
# only by the test views (e.g. the dummy view's TextField) are included, which a
# pre-built production bundle would otherwise tree-shake away.
vaadin.productionMode=false
vaadin.copilot.enable=false
